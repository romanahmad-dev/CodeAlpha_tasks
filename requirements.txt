# Core dependencies for CodeAlpha internship projects
# Hangman and task automation use Python standard library only.

# Stock Portfolio Tracker
requests>=2.31.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.13.0

# Conversational Chatbot
nltk>=3.8.0
spacy>=3.7.0

# After installing, download the spaCy English model:
#   python -m spacy download en_core_web_sm
#
# NLTK data (punkt, stopwords) is downloaded at runtime in conversational_chatbot.py.
# To pre-download manually:
#   python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords')"
